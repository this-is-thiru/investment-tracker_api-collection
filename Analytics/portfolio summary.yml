info:
  name: portfolio summary
  type: http
  seq: 1

http:
  method: get
  url: http://localhost:8080/analytics/user/{{EMAIL}}/portfolio-summary
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
