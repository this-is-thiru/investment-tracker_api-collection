info:
  name: performance metrics
  type: http
  seq: 4

http:
  method: get
  url: http://localhost:8080/analytics/user/{{EMAIL}}/performance-metrics
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
