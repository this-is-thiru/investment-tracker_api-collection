info:
  name: xirr
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/analytics/user/{{EMAIL}}/xirr
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
