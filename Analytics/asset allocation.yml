info:
  name: asset allocation
  type: http
  seq: 2

http:
  method: get
  url: http://localhost:8080/analytics/user/{{EMAIL}}/asset-allocation
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
