info:
  name: Long | Short term
  type: http
  seq: 3

http:
  method: GET
  url: "{{INVESTMENT_TRACKER_SERVICE}}/portfolio/user/:email/assets/holding/SHORT_TERM"
  params:
    - name: email
      value: "{{EMAIL}}"
      type: path
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
