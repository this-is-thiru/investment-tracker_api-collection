info:
  name: Get Portfolio Stocks
  type: http
  seq: 2

http:
  method: GET
  url: "{{INVESTMENT_TRACKER_SERVICE}}/portfolio/user/:email/stocks/all"
  params:
    - name: email
      value: "{{EMAIL}}"
      type: path
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
