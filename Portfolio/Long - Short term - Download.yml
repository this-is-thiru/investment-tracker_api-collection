info:
  name: Long | Short term | Download
  type: http
  seq: 4

http:
  method: GET
  url: "{{INVESTMENT_TRACKER_SERVICE}}/portfolio/user/:email/assets/holding/LONG_TERM/excel"
  params:
    - name: email
      value: "{{EMAIL}}"
      type: path
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{JWT_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
