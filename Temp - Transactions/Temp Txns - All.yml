info:
  name: Temp Txns | All
  type: http
  seq: 1

http:
  method: GET
  url: "{{INVESTMENT_TRACKER_SERVICE}}/temporary-transactions/user/:email/all"
  params:
    - name: email
      value: "{{EMAIL}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
