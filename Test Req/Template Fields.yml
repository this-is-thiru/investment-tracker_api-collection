info:
  name: Template Fields
  type: http
  seq: 4

http:
  method: GET
  url: "{{INVESTMENT_TRACKER_SERVICE}}/helper/template/fields"
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
