info:
  name: CA | Perform
  type: http
  seq: 2

http:
  method: PUT
  url: "{{INVESTMENT_TRACKER_SERVICE}}/corporate-action/user/:email/perform/OCTOBER/2025"
  params:
    - name: email
      value: "{{EMAIL}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
