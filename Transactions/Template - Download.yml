info:
  name: Template | Download
  type: http
  seq: 5

http:
  method: GET
  url: "{{INVESTMENT_TRACKER_SERVICE}}/helper/template"
  headers:
    - name: Origin
      value: https://localhost
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
