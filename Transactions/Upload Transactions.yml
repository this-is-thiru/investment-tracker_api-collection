info:
  name: Upload Transactions
  type: http
  seq: 3

http:
  method: POST
  url: "{{INVESTMENT_TRACKER_SERVICE}}/portfolio/user/:email/upload-transactions?quarter=Q4"
  params:
    - name: quarter
      value: Q4
      type: query
    - name: email
      value: "{{EMAIL}}"
      type: path
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - /Users/pthirumalayadav/Desktop/NEW UPLOAD/TO BE/ZERODHA/Zerodha_2526.xlsx
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
