info:
  name: Upload Transactions
  type: http
  seq: 3

http:
  method: POST
  url: "{{INVESTMENT_TRACKER_SERVICE}}/portfolio/user/:email/upload-transactions"
  params:
    - name: email
      value: "{{EMAIL}}"
      type: path
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - /C:/Users/91630/OneDrive/Desktop/files/Income tax Docs/API Testing/Upload-transcations/test-template.xlsx
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
