info:
  name: Upload Transactions
  type: http
  seq: 3

http:
  method: POST
  url: "{{INVESTMENT_TRACKER_SERVICE}}/portfolio/user/:email/upload-transactions?quarter=Q1"
  params:
    - name: quarter
      value: Q1
      type: query
    - name: email
      value: "{{EMAIL}}"
      type: path
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - /Users/pthirumalayadav/Desktop/DEMAT P&L/FYERS.csv
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
